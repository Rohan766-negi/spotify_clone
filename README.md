# Spotify Clone (HTML & CSS)

A simple **Spotify-like UI clone** built using **HTML5** and **CSS3**.  
This project is only a frontend mock — no backend or real streaming.

## Demo
Open `index.html` locally or view on GitHub Pages (if enabled).

## Features
- Responsive layout (desktop + mobile)
- Navbar, sidebar, playlists, song cards (UI only)
- Clean folder structure

## Tech
- HTML5
- CSS3 (Flexbox & Grid)

## Project Structure
